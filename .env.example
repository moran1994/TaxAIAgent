# API
APP_ENV=development
API_HOST=0.0.0.0
API_PORT=8000

# SQLite path (relative to apps/api)
DATABASE_URL=sqlite:///./data/taxai.db

# LLM — leave empty until configured; never commit real keys
LLM_API_KEY=
LLM_API_BASE=
LLM_MODEL=gpt-4o-mini

# Product copy source (repo-relative from apps/api cwd may vary)
DISCLAIMER_PATH=../../ops/m0/disclaimer-and-refusal.md
